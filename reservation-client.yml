server:
  port: ${PORT:9999}

ribbon:
  eureka:
    enabled: true

zuul:
  ignoredServices: '*'
  routes:
    reservation-service:
      path: /reservation-service/**
      serviceId: reservation-service
      stripPrefix: false
      sensitiveHeaders: Authorization