server:
  port: ${PORT:8010}

info:
  component: Hystrix Dashboard

gendpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true

logging:
  level:
    ROOT: INFO
    org.springframework.web: DEBUG

eureka:
  instance:
    hostname: hystrix-dashboard